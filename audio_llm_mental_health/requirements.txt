# Install on the H100 environment, not in this sandbox (no GPU / no network here).
torch>=2.2
transformers>=4.45
accelerate>=0.34
peft>=0.12
datasets>=2.20
librosa>=0.10
soundfile>=0.12
pandas>=2.2
pyyaml>=6.0
