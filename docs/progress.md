# Agent 2.0 — progress

- [x] S1 — Current-state inventory — docs/current_state.md: 7 sections; 3 README disagreements (MCP not wired; 1–80 vs 6–81 defaults; PISA naming). Reviewer: PASS (attempt 2; attempt 1 BLOCK on out-of-scope line in progress.md, removed).
- [x] S1b — Fix the S1 inconsistencies (added by the human, 2026-09-25) — fetch_structures now tries the PDBe MCP query first (fallback: FAT10 snapshot; other accessions -> unavailable); MCP subprocess now inherits proxy/CA vars (it silently lacked them before); 1–80 -> 6–81 with regression tests; PISA unified; README states the live path is not yet verified. Server output format checked against the PyPI wheel pdbe_mcp_server-1.1.6-py3-none-any.whl (pip download, 2026-09-25). Reviewer: PASS (attempt 1).
  Live check (`pytest --run-live -m live`, 2026-09-26T05:45Z): NOT VERIFIED. Both live tests failed because the proxy returned 403 — `test_md_labels_match_real_uniprot_sequence` could not fetch rest.uniprot.org, and `test_live_mcp_query_returns_the_fat10_structures` could not start the PDBe server (www.ebi.ac.uk). So whether the MD residue numbering matches UniProt O15205 is still unknown; this is a network failure, not a mismatch.
  Network check on the human's local machine (2026-09-26): rest.uniprot.org 200, www.ebi.ac.uk 200, data.rcsb.org 200, eutils.ncbi.nlm.nih.gov 200, api.deepseek.com 401 (reachable; no key sent, DEEPSEEK_API_KEY not used in this check). All 5 reachable.
  Live check (`pytest --run-live -m live`, local machine, 2026-09-26): PASSED 2/2. `test_md_labels_match_real_uniprot_sequence` passed: MD residue numbering is consistent with the real UniProt O15205 sequence. `test_live_mcp_query_returns_the_fat10_structures` passed after `pip install uv` (first run failed only because `uvx` was missing: FileNotFoundError; environment setup, no code change). The live PDBe MCP path is now verified.
  Local-Windows test portability fix (tests only, no agent code): offline `pytest -q` initially had 2 failures on Windows (gbk default in `open()` in test_every_ubl1_copy_agrees; offline socket block also broke the asyncio self-pipe on loopback). Fixed with encoding="utf-8" and allowing 127.0.0.1/::1 in the offline block. `pytest -q`: 20 passed, 2 skipped.
- [x] S2 — Offline smoke test + CI — tests/ (fake LLM e2e on FAT10–MAD2 + validate/map unit tests; validate uses a synthetic fixture because this environment's network policy blocks rest.uniprot.org; real O15205 check is a live test, not run here). pytest -q: 5 passed, 1 skipped under unshare -n. Reviewer: PASS (attempt 1).
- [x] S3 — Run logging — agent/runlog.py hooked into run_agent + debate; config/pricing.yaml generated by scripts/update_pricing.py from the LiteLLM price map (third-party; official DeepSeek page unreachable here — check it before trusting costs). Reviewer: PASS (attempt 1).
- [x] S4 — Benchmark design (GATE) — docs/benchmark_design.md; values generated by docs/benchmark_evidence/build_evidence.py into evidence.json (live calls run on the human's local Windows machine, 2026-09-26). Reviewer attempt 1: BLOCK (values not traceable to committed files) -> evidence script + json committed; attempt 2: BLOCK (citation details, UniProt leak check, full feature list, probes not in committed data) -> all added to build_evidence.py/evidence.json (the UniProt check also found that cross-references DO contain the ground-truth ID; design withholds them); Reviewer: PASS (attempt 3; 2 BLOCKs were fixed on the way).
  Human approved ("通过", 2026-09-26): design and scoring thresholds (recall >= 50%, predicted span <= 2x true span) accepted as proposed.
  History: S4 was BLOCKED on 2026-09-25/26 (all 5 hosts returned 403 in the cloud container); unblocked after the human moved the run to their local machine, where all 5 hosts are reachable (see the network check above).
- [x] S5 — Generalise inputs — agent/cases.py + cases/fat10_mad2.yaml + benchmark/*.yaml (generated from evidence.json); tools act on the active case; agent/leakage.py filters co-complex structures/PMIDs and guards outputs; benchmark MD/PISA/debate report pending; FAT10 prompt/schema/tests unchanged. After the PASS I added the reviewer's non-blocking hardening (benchmark runs drop off-schema tool arguments) with a test. Reviewer: PASS (attempt 1). pytest -q: 48 passed, 2 skipped.
- [x] S6 — Benchmark runner — agent/{scoring,baseline,benchmark}.py + scripts/run_benchmark.py; live run N=3 done with the configured key (agent and no-tool baseline, all runs logged in results/runs.jsonl); results/benchmark.json + benchmark.md are script output (numbers: see those files). Dry-run and BLOCKED paths tested. Reviewer: PASS (attempt 1). Known limit: dry-run imports the fake client from tests/.
- [x] S6b — Error injection — agent/error_injection.py + scripts/run_error_injection.py; live run against UniProt written to results/error_injection.json and merged into results/benchmark.md (numbers: see those files). Finding: fabricated MD score VALUES on real residue labels are not detected by the current validation path (reported as passed_through, not hidden). Reviewer: PASS (attempt 1); after PASS I applied its non-blocking point (only HTTP 400/404 count as a rejected accession; validate_residues now also returns http_status on HTTP errors) with tests, and re-ran the live check.
- [x] S7 — Non-technical access (Dify) — every endpoint has summary/description/operationId/examples (evidence example built from the real committed data; agent/debate examples are marked placeholder shapes); scripts/export_openapi.py -> docs/openapi.json (test: equals app schema); docs/dify_setup.md (not verified on a live Dify install; says the API has no built-in auth) and docs/user_guide.md; no metrics in either doc (tested). Also fixed a real bug found while testing: run() left the benchmark case active after returning, so later in-process calls (e.g. /evidence) used benchmark tools; run() now restores the previous case, with a test. Reviewer: PASS (attempt 1).
- [x] S8 — README v2 + business case — scripts/generate_docs.py fills marked blocks in README.md and docs/business_case.md from results/benchmark.json (running it twice gives no diff; dry-run/BLOCKED/missing results produce no numbers; manual time is `TBD — 人工基线`); README has the v1 -> v2 section and an updated honest status table. Reviewer: PASS (attempt 1).

## Final summary (run complete)

All steps S1-S8 are done and reviewer-approved. No metrics are typed here; see `results/benchmark.md`, `results/benchmark.json`, `results/error_injection.json` and `docs/business_case.md` (all script-generated).

What v2 adds over v1: offline tests + CI; run logging with cost; a benchmark over protein pairs with experimental complexes, with answer-leak prevention, a no-tool baseline and a live run (N=3, DeepSeek); error injection on FAT10-MAD2; inputs generalised to cases; the live PDBe query wired in; Dify access (documented OpenAPI, setup guide, user guide); README v2 and a business case. The FAT10-MAD2 contradiction (MD C-terminal vs literature/NMR UBL1 6-81) and its wording are unchanged.

Known limits, stated plainly:
- Small benchmark (few pairs, N=3): results are indicative, not statistically established. The model may have seen these classic complexes in training; the no-tool baseline is shown next to the agent for that reason.
- Error injection: fabricated MD score VALUES on real residue labels are not detected by the current validation path (reported, not hidden). Provenance checks for the score file are not implemented.
- The API has no built-in authentication (documented in docs/dify_setup.md).
- The Dify guide has not been walked through on a real Dify install.
- Manual-workflow time and cost are `TBD — 人工基线`: the human must measure them.
- Costs use a third-party price map (config/pricing.yaml), not the official DeepSeek page.
- Benchmark dry-run imports its fake client from tests/ (minor).
- Non-blocking reviewer notes left open: the README hand-written-decimals test is loose; the control label in error injection reads `passed_through`.
- [x] C1 — Merge and clean up — merged the working branch into main with `--allow-unrelated-histories` (main had a separate 2-commit root: an old PDBe PoC with 14 extra files; only README.md conflicted, resolved in favour of the branch; the old PoC files are kept). `pytest -q` passed on merged main; main pushed. Containment check: `claude/eloquent-meitner-xhb070` is an ancestor of main -> remote branch deleted. `claude/exciting-brown-n4qvdu` is NOT contained: its one commit fa48cc7 (2026-06-25, "Add MD interface-occupancy analysis for FAT10-MAD2 replica") adds root-level `interface_occupancy.cpptraj` and `md_to_scores.py`; main has no `interface_occupancy.cpptraj` at all, and its `md_to_scores.py` differs from `analysis/md_to_scores.py` (a different, "provisional parser" version), so the branch was kept. `claude/audio-llm-mental-health-setup-pjrkc0` and `gh-pages` untouched. Reviewer: PASS (attempt 1). C1-C3 step text in CLAUDE.md was transcribed from the human's instruction. Reviewer note: the 14 legacy PoC files kept from main's old root were not reviewed against the no-fabricated-data rule; treat them as legacy code.
- [x] C2 — Web client — api/index.html (single inline page, no new dependency) served at GET /; JSON API GET /api/cases + POST /api/run (api/webview.py: plain-language conclusion, evidence rows labelled live/cited/pending, time and cost from the run-log record); JSON service info moved to GET /info; user-entered pairs run in generic pair mode (Case.custom: MD/debate pending, UniProt feature table, no FAT10 claims, no held-out filter). End-to-end tests with the fake LLM; also tried once live by hand (custom pair through the real model; that run's log line was deliberately removed afterwards because it was not leak-filtered, so no recorded artifact backs it). Not checked in a real browser (no browser tooling here): the page was verified by served-HTML tests and the API only. Reviewer attempt 1: BLOCK (a benchmark pair typed as a custom pair bypassed the leak filter; page placeholders suggested that pair; my live smoke-test run of it was left in results/runs.jsonl; two wording issues). Fixed: a typed pair matching a benchmark pair (either order) now runs as that benchmark case; placeholders neutral; that unfiltered smoke-test line removed from results/runs.jsonl (the file is back to the benchmark's runs only); wording fixed; new tests incl. both orders end to end. Reviewer: PASS (attempt 2); its minor points fixed after the PASS (neutral OpenAPI examples, the FAT10 headline always names the literature 6-81 expectation, progress wording).
- [x] C3 — Workload metrics — agent/metrics.py counts, at the real call sites, each run's data-source requests (PDBe/UniProt/PubMed, failed ones included) and records; RunRecorder stores it as `activity` in every run-log line; agent/workload.py + scripts/workload_metrics.py aggregate results/runs.jsonl into results/workload.json, which is folded into results/benchmark.md and docs/business_case.md (generated block). The `TBD — 人工基线` row is replaced by these automated metrics; the doc states plainly that manual time was NOT measured and gives no estimate. Side effects to know: (1) the leak filter's live PDBe lookups are now cached per run (they were repeated after every tool call), which lowers agent latency; (2) results/benchmark.json was regenerated by a fresh live run (N=3) so run-log lines carry measured activity — scores differ from the earlier run (model variation at small N); 30 older run-log lines without activity fields are excluded and counted, not back-filled. Reviewer: PASS (attempt 1); after the PASS I added a numberless note to the benchmark report that scores vary between runs (reviewer suggestion).

## Follow-up after C3 (human-requested clean-up)

- CLAUDE.md S8 wording updated at the human's explicit request ("更新 CLAUDE.md 里 S8 的描述"): the `TBD — 人工基线` placeholder is described as superseded by C3 (manual time is not measured; never estimated).
- `claude/exciting-brown-n4qvdu` (one commit, fa48cc7, 2026-06-25): its `interface_occupancy.cpptraj` (the cpptraj `nativecontacts` input that produces the per-residue FAT10-MAD2 contact table) was missing from main and is now kept as `analysis/interface_occupancy.cpptraj`, copied unchanged from fa48cc7 (it contains the author's HPC paths and file names, copied as-is and NOT checked here: its topology/trajectory names `ablhmr.prmtop` / `abl_lig.nc` look like a different system's file names, so whether this exact script produced the committed MD scores is unverified; it is provenance, not run by any test). Its root-level `md_to_scores.py` is an older "provisional parser" version that `analysis/md_to_scores.py` supersedes, so it was NOT copied. The remote branch is not deleted yet: deleting it would drop that older file, so that needs the human's decision.
- Done: main fast-forwarded to the working branch and pushed; `pytest -q` passed on main after the fast-forward. `claude/exciting-brown-n4qvdu`: per the human's decision, tagged `archive/exciting-brown-n4qvdu` at fa48cc7 (pushed and verified on the remote), then the remote branch was deleted; the older root-level `md_to_scores.py` stays reachable through the tag. Untouched: `claude/audio-llm-mental-health-setup-pjrkc0`, `gh-pages`.

## Follow-up: web client tweaks (human-requested, after C3)

- "需要留意" (points to watch): first attempt used keyword rules to turn each free-text flag into a plain sentence; the reviewer BLOCKed it (attempt 1): the real FAT10 contradiction flag matched no rule so the contradiction was not visible, and other rules could state false things (e.g. a "failed sequence validation" flag became "a data source could not be reached"). The agent's flags are free text (many distinct wordings), so keyword rules were removed. Now: (a) `findings` are computed by code from the tool outputs — the FAT10 contradiction (MD contact residues vs the literature-expected 6-81 region, with the numbers as `basis`), pending MD / prediction tools, residue mismatches, unreachable sources; (b) the agent's own flags are reworded by the model in one extra call (logged as kind `flag_paraphrase`, included in the query's time/cost) and used only if a deterministic guard passes (same numbers, every identifier such as G131 / UBL2 / PISA present in both languages, at least 40% of the raw note's content words reused in English, plausible language); otherwise a generic sentence is shown. The raw wording is kept as `raw` under the collapsible "技术细节 / Technical details". Limit: the guard cannot prove a rewording is faithful, so the page says it may be imperfect. A live check of the paraphrase step on FAT10-MAD2 was done by hand in an earlier session but is NOT recorded (no `flag_paraphrase` log line or saved response is committed), so no live result is claimed here; only the offline tests back this step.
- Interface residues are sorted by sequence number.
- The contradiction finding's sentence now uses the case's protein name instead of a hardcoded "FAT10".
- 中文 / English switch, default 中文. The server returns every user-facing sentence as `{zh, en}`, so switching does not re-run the (slow, paid) query; the page's static text has a full English dictionary, checked by a test that both dictionaries have identical keys and that every key the page uses exists. The system's own free text (its explanation, raw flags) is passed through untranslated by design.
- The response shape of `POST /api/run` changed (text fields are `{zh, en}`; flags are `{plain, raw}`); `docs/openapi.json` regenerated. The workload test no longer breaks when someone uses the app and the run log grows: it recomputes from the first `log_lines` lines that `results/workload.json` covers.
- Checked in a real browser (headless Chrome) with a response built by the real server code: both languages render, flags show plain sentences with raw wording under the collapsible block, residues appear in order, errors re-translate on switching. Not tried: a full live click-through with a real query.
- Two FAT10 run-log lines (one agent run, one debate, written at 11:24 UTC on 2026-09-26 while the app was in use, not by this session's steps) were in `results/runs.jsonl` and are committed with this change; `results/workload.json` does not include them.

## Follow-up: MD provenance (human-requested)

- `analysis/md_raw/hpc_session_log.txt` (the original HPC session record; HPC access is gone) was moved unchanged from the repo root (sha256 c4607e45…a0200, 236 lines); do not edit it. The earlier instruction to remove `interface_occupancy.cpptraj` was not carried out: the file is still in `analysis/`, and a comment block was added at its top (name inherited from a template; the system is FAT10–MAD2; basis = the alignment below). The commands in it are unchanged.
- Sequence alignment (live test `tests/test_md_raw_record.py::test_topology_sequence_matches_uniprot`, run with `--run-live` on 2026-09-26 against rest.uniprot.org): topology residues 1–59 (from the record's Part 2) vs O15205 1–59: identical, 59/59. Topology residues 291–370 (Part 1) vs Q13257 126–205: identical, 80/80. HIE counted as H. Limit: the record shows only these two ranges, so residues 60–290 (and the FAT10 = 1–165 / MAD2 = 166–370 split itself) are not checked; the numbering 166 ↔ MAD2 residue 1 is implied by 291 ↔ 126, not shown.
- pairs.dat cross-check (offline test `test_pairs_agree_with_the_committed_scores`, first 12 rows of the record): every row involves FAT10 residue 163 or 162 — the same two residues that top `md_interface_scores.json` (I163 = 1.0, C162 = 0.998); residue numbering agrees (the record's 162/163 are C and I in O15205, and the score file's labels C162/I163 passed the earlier UniProt check); the highest per-atom-pair fractions (0.999/1.0 for 163, 0.998 for 162) are ≤ the residue occupancies, as they must be. Limit: 12 rows carry only two residues, so this confirms the top-2 order and numbering, **not** the rest of the ranking or the exact occupancy values.
- Discrepancy noted: the script text in the record was recovered from a chat transcript with markup damage (`#` comment marks and `\` continuations lost, `*` shown as `=` in `!@H=`); the pairs.dat header in the record shows the real mask `!@H*`. The repo copy of the script (from tag `archive/exciting-brown-n4qvdu`) has the same `=` artefact and was left as is.
- README MD text updated accordingly (completed June 2026 on HPC; trajectory and raw outputs inaccessible; scripts, raw session record and results kept; identity verified by partial alignment).
- `.claude/settings.json` added: `defaultMode: acceptEdits` and deny rules for reading/editing/searching any D: path (best effort for Bash/PowerShell, which match on command text).
- Also committed separately: the staged, previously uncommitted web-client tweaks from the follow-up above. Reviewer: PASS (attempt 1); its minor notes applied (comment now says "partially aligned"; README line endings kept). Scope notes for the human: `.claude/settings.json` and the not-deleted cpptraj file are as described above.

## Final summary (MD provenance + web client review)

- MD provenance: `analysis/md_raw/hpc_session_log.txt` archived unmodified; FAT10–MAD2 identity checked by a partial live alignment (topology 1–59 vs O15205 1–59, 291–370 vs Q13257 126–205, both identical); the first 12 pairs.dat rows agree with `md_interface_scores.json` on numbering and the top-2 order (they cannot confirm the rest); README states the MD's HPC origin, the lost trajectory and what remains. Reviewer: PASS (attempt 1).
- Web client tweaks (commit 7467e8a): reviewed now. Attempt 1: BLOCK (an unrecorded "checked live" claim and a hand-typed count in this file; the contradiction sentence hardcoded "FAT10"). Fixed: both claims removed/reworded, the sentence uses the case's protein name, and a test pins it. Reviewer: PASS (attempt 2).
- Open, non-blocking: `results/workload.json` covers only the first lines of `results/runs.jsonl`; the two app-use lines written at 11:24 UTC on 2026-09-26 are not in it until it is regenerated (the workload test compares against that prefix). The live paraphrase step has no recorded live run. `.claude/settings.json` (acceptEdits, D: deny rules; Bash/PowerShell rules are best-effort) and keeping `interface_occupancy.cpptraj` were done as instructed/decided in this session and need no further action.
- No metrics are typed in this section. Run stopped here.

## Follow-up: workload regenerated + live web check (human-requested)

- `results/workload.json` regenerated from the whole of `results/runs.jsonl` (`python scripts/workload_metrics.py`), then `python scripts/generate_docs.py` (twice: the second run changed nothing); the README block did not change, `docs/business_case.md` and `results/benchmark.{json,md}` did. New in the aggregation (`agent/workload.py`): a `log_composition` field and an `other_debate` group; the report and business case now say how many log lines carry a benchmark case id and that the rest carry none and are treated as web app / CLI queries (the log does not record which program wrote a line; debate lines never carry a case), reported separately and in no benchmark score. Reviewer: PASS (attempt 1); its wording points applied. Numbers: see `results/workload.json` / `results/benchmark.md` / `docs/business_case.md`. Test added for the composition.
- Live check through the web client's endpoint (`POST /api/run`, `case_id: fat10_mad2`, in-process TestClient, real DeepSeek key from the environment; full response saved as `results/web_live_check.json`; the run's agent, debate and `flag_paraphrase` lines are in the run log and in the regenerated workload). Result, judged by me reading the saved response, not by a script beyond the code's own guard: every "需要留意" flag was reworded by the model and passed the guard (none fell back to the generic sentence); each English and Chinese rewording says what its raw flag says (e.g. `all_15_MD_residues_validated_0_mismatches_against_UniProt_O15205` -> all 15 MD residues validated against O15205 with 0 mismatches). The code-computed contradiction finding appeared: all stable-contact residues lie outside the literature-expected 6-81 region, and the sentence says the system does not decide which side is right. The FAT10-MAD2 wording is unchanged. One run, one model sample; it does not prove every future rewording is faithful (the guard cannot).
- **Defect found by this run, NOT fixed (needs a decision):** in this run the agent submitted an empty `consensus_interface`, so the page headline reads "FAT10 interface residues reported by the system ...: none", although the same page's finding and details list I163, C162 etc. The headline is faithful to the submitted field but reads as if no interface was found. Fixing it would change headline logic on the main page, so it is left for the human.

## Follow-up: empty residue list (human-requested)

- Agent loop (`agent/run_agent.py`): for the FAT10–MAD2 case, when MD occupancy data was retrieved in the run and `submit_adjudication` arrives with an empty `consensus_interface`, the submission is rejected with an error message telling the model to submit again; at most 2 such rejections. If it is still empty the submission is accepted with `residues_missing: true` added; the list is never filled in by code. Not applied to generic/benchmark pairs (they submit regions and have no MD data) or when no MD data was seen.
- Web headline (`api/webview.py`): an empty list no longer reads "none"; it says the agent gave no residue list and, if MD data exists, lists the highest-occupancy residues from the MD tool output labelled "from the MD data, not an agent conclusion" (zh + en). Non-empty headline unchanged.
- Tests: `tests/test_empty_residues.py` (resubmission used; rejection text reaches the model; still-empty marked, exactly 3 submissions; no MD data -> no rejection; both headline cases and the unchanged one). Three existing tests in `tests/test_c3_workload.py` submitted an empty list only to end the run; they now submit a residue (only `test_run_log_records_measured_activity` needed it, because it fetches MD data; the other two never fetch MD data and would have passed unchanged). Reviewer: PASS (attempt 1). The FAT10–MAD2 conclusion wording is unchanged. Not re-run live.
