# Agent 2.0 — progress

- [x] S1 — Current-state inventory — docs/current_state.md: 7 sections; 3 README disagreements (MCP not wired; 1–80 vs 6–81 defaults; PISA naming). Reviewer: PASS (attempt 2; attempt 1 BLOCK on out-of-scope line in progress.md, removed).
- [ ] S1b — Fix the S1 inconsistencies (added by the human, 2026-09-25)
- [x] S2 — Offline smoke test + CI — tests/ (fake LLM e2e on FAT10–MAD2 + validate/map unit tests; validate uses a synthetic fixture because this environment's network policy blocks rest.uniprot.org; real O15205 check is a live test, not run here). pytest -q: 5 passed, 1 skipped under unshare -n. Reviewer: PASS (attempt 1).
- [x] S3 — Run logging — agent/runlog.py hooked into run_agent + debate; config/pricing.yaml generated by scripts/update_pricing.py from the LiteLLM price map (third-party; official DeepSeek page unreachable here — check it before trusting costs). Reviewer: PASS (attempt 1).
- [ ] S4 — Benchmark design (GATE)
  BLOCKED: every S4 ID (UniProt, ground-truth PDB complex) and each ground-truth interface
  "as reported by PDBe" must come from a live API call, but this environment's network
  policy returns 403 for all needed hosts (checked 2026-09-25 from the container and via
  WebFetch): rest.uniprot.org, www.ebi.ac.uk (PDBe API + graph-api), ftp.ebi.ac.uk (SIFTS),
  data.rcsb.org / search.rcsb.org, files.wwpdb.org, pdbj.org, eutils.ncbi.nlm.nih.gov.
  Writing IDs from memory would violate the no-fabrication rule, so no design doc was
  written. Unblock: allow at least rest.uniprot.org, www.ebi.ac.uk and
  eutils.ncbi.nlm.nih.gov (plus api.deepseek.com for live agent runs in S6), then resume
  at S4. S5 depends on S4 and was not started.
- [ ] S5 — Generalise inputs
- [ ] S6 — Benchmark runner
- [ ] S7 — Non-technical access (Dify)
- [ ] S8 — README v2
